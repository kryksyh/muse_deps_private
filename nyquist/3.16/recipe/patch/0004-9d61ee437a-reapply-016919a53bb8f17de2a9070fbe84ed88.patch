Audacity commit 9d61ee437a
Subject: Reapply 016919a53bb8f17de2a9070fbe84ed88fb27e175
Reapplies: 016919a53bb8f17de2a9070fbe84ed88fb27e175 Bug1223: (correction).  Fix new potential crash in following pointer.

diff --git a/nyqsrc/sound.c b/nyqsrc/sound.c
index 0a9e9f3..6078502 100644
--- a/nyqsrc/sound.c
+++ b/nyqsrc/sound.c
@@ -545,6 +545,7 @@ void snd_list_unref(snd_list_type list)
             break; // the rest of the list is shared, nothing more to free
         }
 
+        next = NULL;
         // list nodes either point to a block of samples or this is the 
         // last list node (list->block == NULL) which points to a suspension
         // lists can also terminate at the zero_block, which is an infinite
