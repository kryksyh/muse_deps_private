# Version lives here, not in the app.
set(DEP_VERSION 0.74.0)

# Build-time tool: the codestyle gate formats/checks with exactly this version
# (config is tuned to it). Replaces a committed Linux-only binary.
set(DEP_KIND tool)
