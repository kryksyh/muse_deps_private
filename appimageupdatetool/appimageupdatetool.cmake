# Pinned version — single source of truth for this dep (mirror snapshot date:
# upstream publishes only moving `continuous` builds).
set(DEP_VERSION 2026.06.13)

# Packaging tool (linux AppImage). Binary-dist: upstream's continuous AppImage
# builds, mirrored immutably; the recipe spec carries the per-arch pins.
set(DEP_KIND tool)
