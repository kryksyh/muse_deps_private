# Pin is a short sha (w3c/mnx is a living spec with no releases). Source-delivery:
# provides the MNX JSON schema (docs/mnx-schema.json) that mnxdom embeds; its own
# CMake is not used.
set(DEP_VERSION d513cf7)
set(DEP_KIND source)
